language: node_js
  
cache: npm

node_js:
  - "15"



script: 
  - npm install
  - npm run test
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  local_dir: build
  github_token: $github_token
  on:
    branch: master

